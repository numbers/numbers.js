/**
 * The core object holds any method that must be used by more than one numbers.js
 * data structure, such that none of them have to be dependent on one another.
 * Most data structures are dependent on core, but it has no dependencies.
 */

var core = exports;